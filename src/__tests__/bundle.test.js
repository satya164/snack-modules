/* eslint-env jest */
/* @flow */

it('creates bundle for latest package', () => {});

it('creates bundle for specific version', () => {});

it('creates bundle for latest package with deep path', () => {});

it('creates bundle for specific version with deep path', () => {});
